export interface QuestionCheckType {
  number: number;
}
export interface ChatCurrentType {
  chatBtnCurrent: number;
}
export interface QuestionValueType {
  id: number;
  name: string;
  questionNumber: string;
  questionTitle: string;
  question: string[];
  questionResult: number;
}
